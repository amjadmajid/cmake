# set the minimum required CMake verison
cmake_minimum_required (VERSION 2.6)
# set the project name
project (app)
# build the executable `app` form the source file `main.c`
add_executable(app main.c)
